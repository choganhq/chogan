# اضافه کردن بازی جدید

<div dir="rtl">

بازی جدید یعنی یک پوشه‌ی تازه زیر `www/games/` و یک ورودی در `www/games.json`. نه ریپوی جدید، نه اپ جدید، نه ثبت دوباره در اف‌دروید. کاربری که چوگان را نصب کرده، بازی جدید را با آپدیت بعدی می‌گیرد.

## پنج قدم

**۱. پوشه را از قالب کپی کن.** شناسه‌ی بازی سه جا باید یکی باشد: نام پوشه، کلید `id` در `games.json`، و شناسه‌ای که به `Chogan.game` می‌دهی. همین شناسه پیشوند ذخیره‌سازی هم هست.

```bash
cp -r template/game www/games/my-game
```

**۲. `www/games/my-game/index.html` را بنویس.** ساختارش در قالب توضیح داده شده. سه قانون که همه‌ی بازی‌ها رعایتشان می‌کنند:

- منطق خالص بازی بین `ENGINE START` و `ENGINE END` می‌آید. `tools/test.js` همان بخش را از دل فایل بیرون می‌کشد و در نود تست می‌کند، پس نسخه‌ی دوم کد برای تست وجود ندارد.
- هیچ‌وقت `localStorage` را مستقیم صدا نزن. فقط `ctx.store` که پیشوند `chogan.<id>.` می‌زند. `localStorage.clear` در کل اپ ممنوع است چون همه‌ی بازی‌ها روی یک اوریجین‌اند و یک خط، پیشرفت همه را می‌سوزاند.
- `history.pushState` و `replaceState` ممنوع‌اند. دکمه‌ی بازگشت سخت‌افزاری اندروید به تاریخچه‌ی وب‌ویو نگاه می‌کند؛ هر ورودی اضافه یعنی کاربر به‌جای رسیدن به منو وسط بازی گیر می‌کند.

**۳. آیکون `icon.svg` را بکش.** مربع ۹۶ در ۹۶ با گوشه‌ی گرد، پس‌زمینه‌ی روشن از خانواده‌ی رنگ تأکید بازی. بدون فایل بیرونی و بدون PNG.

**۴. ورودی را به `www/games.json` اضافه کن.**

```json
{
  "id": "my-game",
  "name":    { "fa": "بازی من", "en": "My Game" },
  "summary": { "fa": "یک جمله، کوتاه.", "en": "One short line." },
  "path": "games/my-game/index.html",
  "icon": "games/my-game/icon.svg",
  "color": "#A57BD1",
  "added": "2026-09-05",
  "files": []
}
```

`path` و `icon` مسیر کامل فایل‌اند، نه پوشه. سرویس‌ورکر دقیقاً همین رشته‌ها را کش می‌کند و منو هم دقیقاً همین‌ها را لینک می‌کند، پس هیچ حدسی وسط کار نیست. اگر بازی فایل جدا لازم داشت — مثلاً یک تصویر — مسیرش را در `files` بگذار.

**۵. نسخه را بالا ببر.** در `www/version.js` هم `APP_VERSION` هم `APP_VERSION_CODE`. اسم کش سرویس‌ورکر به نسخه گره خورده، پس بدون بالا بردن نسخه، کاربرِ قدیمی بازی جدید را نمی‌بیند. یک خط هم به چنج‌لاگ اف‌دروید در `fastlane/metadata/android/*/changelogs/<versionCode>.txt` اضافه کن.

## قبل از PR

```bash
node tools/test.js          # موتور همه‌ی بازی‌ها
tools/browser-check.sh      # همه‌ی صفحه‌ها در کروم بدون سر، بدون خطای کنسول
```

پوشه‌ی `template/game` را نمی‌شود سر جای خودش در مرورگر باز کرد، چون مسیرهای نسبی‌اش
(`../../lib/…`) فرض می‌کنند بازی زیر `www/games/<id>/` نشسته است. اول کپی‌اش کن، بعد تست.

## چیزهایی که هسته برایت انجام می‌دهد

دست نزن و دوباره ننویس:

| کار | راهش |
| --- | --- |
| ذخیره‌سازی | `ctx.store.get/set` و `ctx.autosave` |
| بازگشت به منو | `Chogan.back()`، دکمه‌اش خودکار در سربرگ هست |
| دوزبانگی و رقم فارسی | `ctx.t('key')` و `C.num(n)` |
| صدا و لرزش | `C.feedback('good')` و `C.audio.sfx` و `C.haptic` |
| سکه | `C.coins.can/spend`، درآمد از راه `ctx.finish` |
| دستاورد | `C.achievements.unlock('id')` |
| امتیاز لیگ و آمار | `ctx.finish({ points, coins, won, timeMs })` |
| چالش روزانه | `C.daily(id, date)` که بذر قطعی می‌دهد |
| بهترین رکورد | `C.stats.best(id, key, value, lowerIsBetter)` |
| آموزش اول بازی | `ctx.tutorialIfNew(pages)` |
| پنجره، اعلان، کانفتی، لرزش خطا | `C.ui.*` |

## قانون‌های طراحی

- هدف لمسی حداقل ۴۴ پیکسل. لمس اولویت اول، کیبورد اولویت دوم ولی کامل.
- رنگ و اندازه فقط از توکن‌های `chogan.css`. رنگ خام داخل بازی ننویس، به‌جز جایی که بازی توکن اختصاصی خودش را تعریف می‌کند و دلیلش را کنارش نوشته.
- هیچ منبع بیرونی. نه فونت گوگل، نه سی‌دی‌ان، نه تصویر از اینترنت. اپ باید با اینترنت قطع صددرصد کار کند.
- بازی نیمه‌کاره نباید بسوزد. بعد از هر حرکت معنادار ذخیره کن.
- جلسه‌ی پنج تا پانزده دقیقه، با حس تمام شدن. بدون تایمر انتظار، بدون ارز خریدنی، بدون گاچا.

</div>
