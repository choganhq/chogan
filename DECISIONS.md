# تصمیم‌ها، ریسک‌ها و کارهای باقی‌مانده

<div dir="rtl">

## تصمیم‌هایی که خودم گرفتم

**بدون نود در مسیر بیلد.** هسته‌ی کپسیتور به‌صورت `com.capacitorjs:core` از Maven Central می‌آید و یک تسک گریدل جای `npx cap sync` را گرفته. برای اف‌دروید یعنی بیلدشان فقط گریدل لازم دارد.

**`MainActivity` نوشته شد.** سورس کپسیتور ۸ را کامل گشتم: هیچ مدیریتی برای دکمه‌ی بازگشت سخت‌افزاری ندارد. با اکتیویتی خام، بازگشت وسط بازی کل اپ را می‌بست. بیست خط جاوا این را حل کرد؛ پلاگین `@capacitor/app` را رد کردم چون یک وابستگی برای کاری که بیست خط است.

**نسخه در `www/version.js` است، نه `version.json`.** چون سرویس‌ورکر کش-اول با اسم کش نسخه‌دار خواستی. مرورگر سرویس‌ورکر را فقط وقتی دوباره نصب می‌کند که بایت‌های خودش یا اسکریپت‌های `importScripts` عوض شده باشند. اگر نسخه را `fetch` می‌کردم `sw.js` بین دو انتشار یکسان می‌ماند و کاربر برای همیشه نسخه‌ی قدیمی را می‌دید. یک فایل جاوااسکریپت هر سه مصرف‌کننده را می‌دهد: گریدل با رجکس، صفحه با تگ اسکریپت، سرویس‌ورکر با `importScripts`.

**قالب بازی داخل همین ریپو ماند، نه ریپوی جدا.** تنها دلیل ریپوی جدا دکمه‌ی «Use this template» گیت‌هاب بود که با یک‌اپ‌شدن پروژه بی‌مصرف شد. داخل ریپو، تغییر قرارداد و به‌روزرسانی قالب در یک کامیت اتفاق می‌افتد و امکان واگرایی صفر است.

**تست‌ها موتور را از دل فایل‌های منتشرشده بیرون می‌کشند**، بین `ENGINE START` و `ENGINE END`. اگر کد را برای تست کپی می‌کردم، سر هفته‌ی دوم تست سبز به هیچ معنایی نبود.

**سودوکو وب‌ورکر ندارد.** اندازه گرفتم: بدترین حالت تولید سی میلی‌ثانیه. یک لایه‌ی Blob Worker برای سی میلی‌ثانیه فقط ریسک اضافه بود.

**در چالش روزانه‌ی مین‌روب، خانه‌ی اولین کلیک هم از بذر روز می‌آید.** تخته بعد از اولین کلیک ساخته می‌شود؛ اگر جای کلیک را به کاربر می‌سپردم، تخته‌ی هر بازیکن با بقیه فرق می‌کرد و «یکسان برای همه» دروغ بود.

**حریف‌های لیگ روی دستگاه ساخته می‌شوند و زیر جدول صریح نوشته شده که آفلاین‌اند.** جدول تک‌نفره مرده بود، ولی وانمود کردن به بازیکن واقعی هم درست نبود. مدل داده فیلد نام و کشور و شناسه‌ی ناشناس تصادفی دارد تا اگر روزی همگام‌سازی آمد چیزی عوض نشود.

**`color-mix` هیچ‌جا استفاده نمی‌شود.** وب‌ویوهای قدیمی اندروید نمی‌فهمندش و رنگ کلاً می‌افتد. رنگ‌های نیمه‌شفاف در جاوااسکریپت حساب می‌شوند.

**آستانه‌های لیگ:** برنز تا نقره ۳۰۰، نقره تا طلا ۷۰۰، طلا تا الماس ۱۴۰۰، الماس تا افسانه ۲۶۰۰. با امتیاز معمول هر دور، هفته‌ی اول برنز به نقره چند دور بازی می‌خواهد و افسانه واقعاً سخت است.

**بیست‌ودو تصمیم ریز دیگر** در توضیح و ریویوی هر PR نوشته شده. اگر دنبال دلیل چیزی می‌گردی، اول آنجا را ببین.

## چیزهایی که خواستی و همان‌طور که خواستی نشد

**بازی دفاع از برجِ موجود منتقل نشد؛ از صفر نوشته شد.** گفتی «فایل فعلی رو ببر»، ولی هیچ فایلی در فضای کاری نبود. از صفر نوشتم با همان مشخصات: چهار برج، سه نقشه‌ی ثابت، نقشه‌ی روزانه، پیش‌نمایش موج، بیست موج و حالت بی‌پایان، ستاره و دستاورد. اگر نسخه‌ی قدیمی جایی هست و مکانیک خاصی داشته، بگو تا منتقلش کنم.

**حالت بدون حدس مین‌روب پیاده شد و لازم نشد پلن دوم اجرا شود.** گفته بودی اگر تا حد معقولی شدنی نبود گزینه‌ی معمولی بگذارم؛ شدنی بود. حتی شانزده در سی با نود‌ونه مین در بدترین حالت شانزده میلی‌ثانیه طول می‌کشد. اگر روی سخت‌افزار ضعیف بودجه‌ی زمانی تمام شود، بازی تخته‌ی معمولی می‌دهد و صریح به کاربر می‌گوید.

**سطح سخت نقطه‌بازی روی تخته‌ی چهار در چهار حدود هشتاد درصد می‌برد، نه بیشتر.** تخته‌های زوج به پاریتی حساس‌ترند و بردن قطعی‌شان کمینه‌بیشینه‌ی کامل می‌خواهد که روی گوشی گران است. روی بقیه‌ی اندازه‌ها بین نود تا صد درصد.

**آموزش اول بازی دو یا سه صفحه است، نه بیشتر**، و تصویرسازی‌هایش ساده‌اند. برای بازی‌هایی که قانونشان یک جمله است، آموزش طولانی‌تر مزاحمت است.

## چیزی که روی گوشی واقعی تأیید شد

**اپ بدون مجوز اینترنت بالا می‌آید.** این بزرگ‌ترین ریسک باز پروژه بود و بسته شد. APK امضاشده‌ی `v0.1.1-test` روی دستگاه واقعی نصب و اجرا شد. یعنی وب‌ویو اندروید بار کردن `https://localhost` را وقتی کپسیتور درخواست‌ها را رهگیری می‌کند اجازه می‌دهد، حتی بدون `android.permission.INTERNET` در منیفست. نه صفحه‌ی سفید، نه `ERR_ACCESS_DENIED`.

این نکته‌ای است که موقع نوشتن پروژه منبع قطعی برایش پیدا نکردم و مجبور شدم روی استدلال از سورس تکیه کنم. حالا شواهد تجربی دارد. برای هر پروژه‌ی کپسیتوری دیگری که بخواهد کاملاً آفلاین باشد هم همین نتیجه صادق است.

## ریسک‌هایی که هنوز روی گوشی واقعی سنجیده نشده‌اند

**۱. یک مجوز خودتعریف در فهرست بسته دیده می‌شود.** در بیلد CI معلوم شد بسته علاوه بر `VIBRATE` یک مجوز به نام `io.github.choganhq.chogan.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION` هم دارد. این مجوز سیستمی نیست؛ اندروایکس برای اپ‌هایی که هدفشان API ۳۳ به بالاست خودش آن را با نام بسته‌ی خودمان تعریف می‌کند و فقط بین اجزای همین اپ معنا دارد. اف‌دروید و تنظیمات اندروید ممکن است نشانش بدهند. کاری نمی‌شود کرد و لازم هم نیست، ولی بدان که هست تا غافلگیر نشوی. تست CI فقط مجوزهای `android.permission.*` را می‌سنجد و باید دقیقاً `VIBRATE` باشد.

**۲. صدا و لرزش.** WebAudio باید با اولین لمس باز شود؛ روی وب‌ویو اندروید سیاست‌های خودکارپخش گاهی سخت‌گیرتر است. الگوهای `navigator.vibrate` هم روی دستگاه‌های مختلف فرق می‌کنند و بعضی سازنده‌ها الگوی چندتکه را کوتاه می‌کنند.

**۳. دکمه‌ی بازگشت سخت‌افزاری.** منطقش نوشته و مستدل است ولی هیچ‌وقت روی دستگاه اجرا نشده. سه حالت را تست کن: وسط بازی، روی منو، و وقتی پنجره‌ای باز است.

**۴. کارایی.** دفاع از برج در سرعت دو برابر و مین‌روب شانزده در سی روی گوشی ضعیف. همه‌ی رسم‌ها canvas یا SVG است و بهینه‌سازی خاصی نشده.

**۵. اندازه‌ی هدف لمسی.** خانه‌های سودوکو و خط‌های نقطه‌بازی روی گوشی کوچک. ناحیه‌ی لمس خط‌ها عمداً خیلی پهن‌تر از خود خط است، ولی روی صفحه‌ی پنج‌اینچی باید دیده شود.

**۶. باقی مسیرها هنوز به‌صورت جدی تست نشده‌اند.** اپ بالا می‌آید، ولی گزارش نصب یک جمله بود نه یک سیاهه‌ی تست. موارد دو تا پنج بالا — دکمه‌ی بازگشت در هر سه حالت، صدا و لرزش، کارایی در سرعت دو برابر، لمس طولانی پرچم، و اندازه‌ی هدف لمسی — هنوز جداگانه تأیید نشده‌اند.

## چه چیزی همین حالا کار می‌کند

- **نسخه‌ی وب زنده است:** <https://choganhq.github.io/chogan/>. پیجز فعال شد، ورک‌فلو اجرا شد، همه‌ی فایل‌ها از فونت تا هر چهار بازی با کد ۲۰۰ برمی‌گردند و صفحه بدون خطای کنسول بار می‌شود.
- **تست روی هر PR و هر پوش سبز است:** ۲۶۹ بررسی موتور بازی‌ها، باز شدن هر پنج صفحه در کروم بدون سر، بیلد دیباگ اندروید، بررسی محتوای APK و بررسی مجوزها.
- **توضیح و موضوع‌های ریپو پر شده‌اند.**

## زنجیره‌ی انتشار کامل تست شد

سکرت‌های امضا تعریف شدند و تگ `v0.1.1-test` تا آخر سبز رفت: تست، بیلد ریلیز، امضا، تأیید امضا با `apksigner`، فایل چک‌سام، و پیوست شدن به Releases به‌صورت pre-release. APK منتشرشده را دانلود و بررسی کردم — چک‌سام می‌خواند، بلوک امضای نسخه‌ی ۲ و ۳ سر جایش است، و هر چهار بازی و فونت داخل بسته‌اند. حجم: ۳٫۰۴ مگابایت.

کارهای باقی‌مانده:

۱. انتشار آزمایشی `v0.1.1-test` را وقتی دیگر لازمش نداشتی پاک کن:
```bash
gh release delete v0.1.1-test --cleanup-tag --yes
```

۲. اثر انگشت گواهی امضا را جایی امن نگه دار؛ مرحله‌ی اف‌دروید لازمش می‌شود:
```
SHA-256  c3f70a1af8a45558678d1d9b413415d1b0a4c3208835ce78c1f37c4a3a008839
```

۳. باقی موارد تست دستی را روی گوشی بزن: دکمه‌ی بازگشت در هر سه حالت، صدا و لرزش، سرعت دو برابر دفاع از برج، لمس طولانی پرچم در مین‌روب، و اندازه‌ی هدف لمسی سودوکو و نقطه‌بازی.

۴. انتشار واقعی با `git tag v0.1.0 && git push origin v0.1.0`، بعد ثبت در اف‌دروید. متادیتا آماده است. یادت باشد APK اف‌دروید و APK گیت‌هاب با کلیدهای متفاوت امضا می‌شوند و روی هم آپدیت نمی‌شوند.

۵. اگر نسخه‌ی قدیمی دفاع از برج جایی هست، بفرست تا مکانیک‌هایش را منتقل کنم.

</div>
